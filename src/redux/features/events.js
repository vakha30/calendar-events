const initialState = {
  loading: false,
  items: [],
  error: null,
};

const events = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default events;
